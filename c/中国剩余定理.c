#include<stdio.h>
#include<math.h>
#define N 10
int main(void)
{
	int value[N];
	int number;
	int a;
	printf("获取n的值\n");
	if(a=0;a<N;a++)
	{
		scanf("%d",&value[N]);
		number=8+(15*value[N]);
		printf("结果：%d\n",number);	
	}
	printf("计算结束\n");
	return 0;
}
